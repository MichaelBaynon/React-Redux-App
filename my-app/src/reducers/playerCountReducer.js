const initialState = {
    playerCount: [],
    test: 'Its working!'
}

export const reducer = (state = initialState, action) => {
    switch(action.type) {
        default: 
        return state;
    }
}